# Правила для проекта Skypro Wallet - Учет расходов и доходов

## 🎯 Контекст проекта
Это приложение для учета личных финансов с React Frontend и API Backend.
Основные функции: добавление доходов и расходов, категоризация трат, аналитика, бюджетирование, отчеты.

## 📁 Структура проекта
```
src/
├── components/     # Переиспользуемые компоненты
├── pages/         # Страницы приложения
├── services/      # API и внешние сервисы
├── assets/        # Статические ресурсы
├── utils/         # Утилиты для работы с финансами
```

## ⚛️ React компоненты

### Стиль кода
- Используй ES6+ синтаксис
- camelCase для переменных и функций
- PascalCase для компонентов
- UPPER_SNAKE_CASE для констант
- Используй const для компонентов

### Структура компонентов
- Один компонент на файл
- Используй .jsx расширения
- Экспортируй по умолчанию компонент
- Группируй импорты: React, внешние библиотеки, внутренние модули

### Hooks правила
- Используй useState для локального состояния
- Используй useEffect для побочных эффектов
- Создавай кастомные хуки для переиспользуемой логики
- Следуй правилам хуков React

### Пропсы
- Деструктурируй пропсы в параметрах
- Валидируй пропсы
- Передавай данные через props, не через глобальное состояние

## 🎨 Стилизация (Styled Components)

### Соглашения
- Используй styled-components для всех стилей
- Создавай .styled.js файлы рядом с компонентами
- Именуй styled компоненты в PascalCase
- Группируй стили по компонентам

### Структура styled файлов
```javascript
import styled from "styled-components";

export const ComponentContainer = styled.div`
  // стили
`;

export const ComponentTitle = styled.h2`
  // стили
`;
```

### Responsive design
- Используй медиа-запросы в styled-components
- Следуй mobile-first подходу
- Используй CSS Grid и Flexbox

## 🌐 API и сервисы

### Структура API
- Все API вызовы в папке services/
- Используй axios для HTTP запросов
- Группируй API методы по сущностям (authApi, transactionsApi, categoriesApi)
- Добавляй JSDoc комментарии к API методам

### Обработка ошибок
- Используй try-catch для всех асинхронных операций
- Логируй ошибки с контекстом
- Показывай понятные сообщения пользователю
- Обрабатывай состояния загрузки

### Пример API метода
```javascript
/**
 * Получение транзакции по ID
 * @param {string|number} id - ID транзакции
 * @returns {Promise} Данные транзакции
 */
getTransaction: async (id) => {
  try {
    const response = await api.get(`/api/transactions/${id}`);
    return response.data.transaction;
  } catch (error) {
    console.error("Ошибка при получении транзакции:", error);
    throw new Error(error.response?.data?.error || "Ошибка при загрузке транзакции");
  }
}
```

## 💰 Финансовая логика

### Типы транзакций
Используй только эти типы:
- "income" - доход
- "expense" - расход

### Категории по умолчанию
#### Доходы:
- "Зарплата"
- "Фриланс"
- "Инвестиции"
- "Подарки"
- "Прочие доходы"

#### Расходы:
- "Еда"
- "Транспорт"
- "Развлечения"
- "Здоровье"
- "Одежда"
- "Коммунальные услуги"
- "Образование"
- "Прочие расходы"

### Структура транзакции
```javascript
{
  _id: string,
  amount: number,
  type: "income" | "expense",
  category: string,
  description?: string,
  date: string,
  userId: string
}
```

### Компоненты финансового приложения
- TransactionsList - список транзакций
- TransactionForm - форма добавления/редактирования транзакции
- Balance - отображение баланса
- CategoryFilter - фильтр по категориям
- Analytics - аналитика и графики
- BudgetTracker - отслеживание бюджета

## 🛡️ Аутентификация

### Правила безопасности
- Храни токены в localStorage
- Проверяй аутентификацию перед API вызовами
- Используй ProtectedRoute для защищенных страниц
- Очищай данные при logout

### Структура аутентификации
- authApi в services/api.js
- ProtectedRoute компонент для защиты маршрутов
- Состояние isAuth в главном App компоненте

## 🧭 Роутинг

### Маршруты приложения
- `/` - главная страница (дашборд с балансом и последними транзакциями)
- `/transactions` - список всех транзакций
- `/analytics` - аналитика и отчеты
- `/budget` - управление бюджетом
- `/categories` - управление категориями
- `/profile` - профиль пользователя
- `/login` - страница входа
- `/register` - страница регистрации
- `/transaction/:id` - просмотр/редактирование транзакции
- `/add-transaction` - добавление новой транзакции
- `/exit` - страница выхода
- `*` - 404 страница

### Правила навигации
- Используй React Router Dom v6+
- Защищай приватные маршруты через ProtectedRoute
- Передавай состояние через props, не через URL

## 📊 Работа с данными

### Форматирование валют
- Используй Intl.NumberFormat для форматирования сумм
- По умолчанию используй рубли (RUB)
- Поддерживай разные валюты в будущем

### Работа с датами
- Используй dayjs или date-fns для работы с датами
- Группируй транзакции по периодам (день, неделя, месяц)
- Поддерживай фильтрацию по датам

### Вычисления
- Баланс = сумма доходов - сумма расходов
- Группировка по категориям для аналитики
- Расчет процентного соотношения категорий

## 📝 Общие правила

### Чистый код
- Следуй принципам DRY и KISS
- Создавай небольшие, фокусированные функции
- Используй понятные имена переменных
- Добавляй комментарии для сложной бизнес-логики

### Обработка состояний
- Минимизируй локальное состояние
- Используй useReducer для сложной логики состояния
- Избегай мутации состояния
- Показывай состояния загрузки и ошибок

### Производительность
- Используй React.memo для оптимизации
- Применяй useMemo и useCallback где необходимо
- Избегай лишних ре-рендеров
- Оптимизируй списки транзакций для больших данных

## 🧪 Тестирование и качество

### ESLint
- Следуй настройкам ESLint проекта
- Исправляй все предупреждения линтера
- Используй современные ES6+ возможности

### Отладка
- Используй console.error для ошибок
- Логируй важные операции (добавление, удаление, редактирование транзакций)
- Добавляй значимые error messages

## 🚀 Git и разработка

### Conventional Commits
- feat: новая функциональность
- fix: исправление багов
- style: изменения стилей
- refactor: рефакторинг кода
- docs: изменения документации

### Структура коммитов
```
feat(transactions): добавить создание новой транзакции
fix(api): исправить обработку ошибок в transactionsApi
style(balance): обновить стили компонента баланса
```

### Ветки
- main - основная ветка
- feature/* - новые функции
- fix/* - исправления
- hotfix/* - критические исправления

## 📱 Адаптивность
- Поддерживай мобильные устройства (320px+)
- Используй responsive подход
- Тестируй на разных разрешениях экрана
- Адаптируй формы и таблицы для мобильных устройств

## 🎯 Приоритеты разработки
1. Функциональность учета транзакций (добавление, редактирование, удаление)
2. Отображение баланса и основной аналитики
3. Удобство пользователя (UX/UI)
4. Расширенная аналитика и отчеты
5. Производительность
6. Безопасность
7. Адаптивность

Всегда отвечай на русском языке и предоставляй конкретные решения для приложения учета финансов.

Ты — профессиональный верстальщик на React. Используешь MCP-сервер Figma для получения точных данных из макета.

Сверстай компонент строго по Figma.

###  Обязательные инструкции:
1. Извлекай все данные из макета Figma через MCP API:
– spacing (padding, margin)
– colors (fill, stroke, background, text)
– font-size, line-height, font-family
– border-radius, box-shadow
– изображения, иконки (nodeId)

2. Работа с изображениями и иконками:
– Проверь, существует ли путь (например, public/icons)
– Если нет — сообщи, что требуется предварительное создание вручную
– Не пытайся скачать до завершения создания папки
– Ошибки вида ENOENT считаются критическими. Не подавляй их, а логируй с указанием nodeId
– Путь к изображениям указывай корректный (/public/...) или через next/image если используется Next.js

4. Если используется Next.js:
– Для картинок используй компонент <Image> из next/image
– Указывай src, width, height, alt
– Путь должен быть совместим с public/

5. Семантическая верстка:
– Используй section, article, nav, main, aside, footer и т.д.
– Верстай строго по иерархии Figma
– Не добавляй ненужные обёртки или классы

### Особые условия:
– Figma — единственный источник истины. Не интерпретируй макет
– Если значение не найдено — не выдумывай, а помечай как временное решение
- Верстай в данной папке макет из Figma. 
- Можно использовать html, CSS, JavaScript, React. Используй БЭМ-язык. 
- При работе с Figma используй абсолютные пути. 
- При написании кода проекта - трансформируй абсолютные пути в относительные	
